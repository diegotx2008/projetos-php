# Geek Portal

Portal PHP para catálogo de filmes, séries e animes.

## Instalação

```bash
composer install
php -S localhost:8000 -t public
```

Configure as variáveis de ambiente em `.env` antes de conectar ao banco de dados ou à API do TMDB.
